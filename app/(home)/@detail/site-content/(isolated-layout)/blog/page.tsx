export default function BlogPage() {
  return (
    <div id="detail" className="container flex grow h-full items-center justify-center mx-auto px-4 py-8 shrink-0">
      <span className="opacity-50 text-sm">
        Görüntülemek için bir yazı seçiniz.
      </span>
    </div>);
}