import SectionWrapper from "@/components/section-wrapper";

export default function Client() {
  return (
    <SectionWrapper className="flex flex-col h-full items-center justify-center">

      <p>Görüntülemek istediğiniz mesajı seçiniz.</p>

    </SectionWrapper>
  )
}